{
  "name": "bearfish",
  "version": "0.8.0",
  "description": "BearFish Firefox extension development tooling",
  "private": true,
  "scripts": {
    "firefox": "web-ext run",
    "dev": "web-ext run",
    "start": "web-ext run",
    "lint": "web-ext lint",
    "test": "node --test"
  },
  "devDependencies": {
    "web-ext": "^10.6.0"
  }
}
